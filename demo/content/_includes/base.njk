<!DOCTYPE html>
<!-- site.url = {{ site.url }} -->
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>{{ title or page.fileSlug or site.title }}</title>
  <meta property="og:title" content="{{ title or page.fileSlug or site.title }}">
  <meta property="og:description" content="{{ description or subtitle }}">
  {%- if thumbnail %}
  <meta property="og:image" content="{{ site.url.slice(0, -1) }}{{ thumbnail }}">
  <meta property="twitter:image" content="{{ site.url.slice(0, -1) }}{{ thumbnail }}">
  {% endif -%}
  {%- if atom %}
  <link rel="alternate" href="{{ atom }}" type="application/atom+xml" title="{{ title or page.fileSlug or site.title }}">
  {% endif -%}
  <meta property="generator" content="{{ eleventy.generator }}">
  <link rel="stylesheet" href="/gallery.css">
  <link rel="stylesheet" href="/style.css">
</head>
<body>
  <header>
    <nav>
      <ul>
        <li><a href="/">home</a></li>
        <li><a href="/gallery">gallery</a></li>
        <li><a href="https://github.com/nycki93/eleventy-image-gallery">source code</a></li>
      </ul>
    </nav>
  </header>
  <main>
    <h1>{{ title or page.fileSlug }}</h1>
    {{ content | safe }}
  </main>
</body>
</html>