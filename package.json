{
  "name": "eleventy-image-gallery",
  "version": "1.2.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "add-images": "node add-images.js",
    "build": "npm run build:images && npm run build:eleventy",
    "build:demo": "DEMO=1 npm run build:images && eleventy",
    "build:images": "npm run add-images",
    "build:eleventy": "eleventy",
    "serve": "eleventy --serve",
    "serve:demo": "DEMO=1 eleventy --serve"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^3.1.2",
    "@11ty/eleventy-plugin-ejs": "^1.0.0",
    "@types/node": "^24.10.1",
    "sharp": "^0.34.5"
  },
  "dependencies": {
    "htmltidy2": "^1.2.0"
  }
}
